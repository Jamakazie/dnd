{% extends 'admin_template.html'%}
{% block main %}
	<div id='characters'>
		<table class = "table table-striped">
			<thead>
				<tr>
					<th>Name</th>
					<th>Race</th>
					<th>Level</th>
					<th>Class</th>
				</tr>
			<thead>
			<tbody>
	
				{% for c in characters %}
					<span id="{{c.pk}}" class="ajax_view">
						<tr class="ajax_view" id="{{c.pk}}">
							<th>{{c.name}}</th>
							<th>{{c.race}}</th>
							<th>{{c.level}}</th>
							<th>{{c.cclass}}</th>
						</tr>
					</span>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
