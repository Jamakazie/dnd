{% block charinfo %}
	<div id="c_name"> {{ c.name }} </div>
	<div id="c_race"> {{c.race}} </div>
	<div id="c_level">Level: {{ c.level}}</div>
	<div id="c_alignment">{{c.alignment.0}} {{c.alignment.1}} {{ c.alignment.2}} </div>
	<div id="c_attack">Base Attack Bonus: {{c.base_attack_bonus}}</div>

	<div id="c_sheet">
		<div id="c_stats">
			<div id="stats_strength">Str: {{c.stats.strength}}</div>
			<div id="stats_dexterity">Dex:  {{c.stats.dexterity}}</div>
			<div id="stats_constitution">Con:  {{c.stats.constitution}}</div>
			<div id="stats_wisdom">Wis:  {{c.stats.wisdom}}</div>
			<div id="stats_intelliegence">Int:  {{c.stats.intellience}}</div>
			<div id="stats_charisma">Cha: {{c.stats.charisma}}</div>
		</div>
		<div id="c_saves">
			<div id="c_fort">Fort:  {{c.saves.fort}} </div>
			<div id="c_reflex"> Reflex: {{c.saves.reflex}}</div>
			<div id="c_will">Will: {{c.saves.will}}</div>
		</div>
		<div id="c_skills">
			{% for skill in c.skills %}
				<div class="skill_name">{{skill.name}}</div>
				<div class="skill_value">{{skill.value}}</div>
			{% endfor %}
		</div>
		<div id="c_items">
			<table class="table table-striped">
				<thead>
					<tr>
						<th>Slot</th>
						<th>Effect</th>
						<th>Gold</th>
						<th>Cursed?</th>
					</tr>
				<thead>
				<tbody>
					{% for item in c.items%}
						<tr>
							<th>{{item.slot}}</th>
							<th>{{item.effects}}</th>
							<th>{{item.price}}</th>
							<th>{{item.iscursed}}</th>
						<tr>
					{% endfor %}
				</tbody>
			</table>
	</div>
{% endblock %}
