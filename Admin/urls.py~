from django.conf.urls import patterns, url

from Admin import views

urlpatterns = patterns('', 
	urls('^$', views.admin_index, name='admin_index'),
	urls('^Create', views.admin_create, name='admin_create'),
	urls('^View', views.admin_view, name='admin_view'),
)
